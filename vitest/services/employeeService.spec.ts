import { test, describe, expect, vi } from 'vitest';

import { deleteEmployee, upsertEmployee } from '../../src/services/employeeService';


describe("EmployeeServiceTest", () => {

    test('deleteEmployeeSuccess', () => {
    
        //console.log(result);
        expect(1).toBe(1);
    })

    test('upsertEmployee Insert Employee', () => {
    
        //console.log(result);
        expect(1).toBe(1);
    })

    test('upsertEmployee  Update Employee', () => {
    
        //console.log(result);
        expect(1).toBe(1);
    })
});

